# Backend base URL used by the Vite dev proxy (default: http://localhost:8000).
# In Docker, nginx proxies /api and /health to http://backend:8000 directly,
# so this is only relevant for `npm run dev`.
VITE_API_BASE=http://localhost:8000
